{
  "name": "odw-daemon",
  "version": "0.1.0",
  "description": "Local orchestration daemon for open-dynamic-workflows: sandboxed scripts, concurrent agents (default 16, scale to your hardware via config.daemon.maxConcurrency), crash-resumable state.",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./embedded": "./src/embedded.js"
  },
  "bin": {
    "odw-daemon": "src/cli.js"
  },
  "scripts": {
    "test": "node --test \"test/*.js\"",
    "coverage": "c8 --check-coverage --lines 80 --include \"src/**\" --exclude \"src/cli.js\" node --test \"test/*.js\""
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "better-sqlite3": "^12.11.1",
    "commander": "^14.0.2",
    "express": "^5.2.1",
    "odw-core": "^0.1.0",
    "p-queue": "^9.3.0",
    "quickjs-emscripten": "0.32.0",
    "ws": "^8.21.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
